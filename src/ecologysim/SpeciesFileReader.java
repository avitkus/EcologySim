package ecologysim;

import java.io.IOException;
import java.nio.file.Path;
import java.util.Optional;

public class SpeciesFileReader {
	static Optional<IAnimalSpecies[]> processAnimals(Path file) throws IOException {
		return Optional.empty();
	}
	
	static Optional<IPlantSpecies[]> processPlants(Path file) throws IOException {
		return Optional.empty();
	}
}
